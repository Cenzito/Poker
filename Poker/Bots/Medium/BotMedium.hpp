#ifndef BOTMEDIUM_HPP
#define BOTMEDIUM_HPP
#include "../Bot.hpp"

class BotMedium /*: public Bot */{
public:

    BotMedium();
    /*
    BotMedium(const std::string& name, int initialChips, int level) : PokerPlayer(name, initialChips) {
        this -> level = level;
    };*/

};

#endif // BOTMEDIUM_HPP
